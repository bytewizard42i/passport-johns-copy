# ----- Node / Yarn -----
node_modules/
.yarn/cache/
.yarn/install-state.gz
.yarn/build-state.yml
.yarn/unplugged/
.pnp.*
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ----- Build outputs -----
dist/
build/
out/
.turbo/
*.tsbuildinfo

# ----- Compact artifacts -----
src/managed/
src/gen/
gen/

# ----- Vite / frontend -----
.vite/
*.local

# ----- Test / coverage -----
coverage/
reports/
.vitest-result.json

# ----- IDE / OS -----
.vscode/*
!.vscode/settings.json.example
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# ----- Local env / secrets -----
.env
.env.local
.env.*.local
*.pem
*.key
*-secret.json

# ----- Runtime config overrides (keep public/config.json, ignore user local copies) -----
didz-ui/public/config.local.json

# ----- Docker volumes -----
.docker-data/

# ----- Midnight private state (browser LevelDB simulators leak here during tests) -----
private-state-store/

# Windows Mark-of-the-Web sidecars (never commit)
*:Zone.Identifier
